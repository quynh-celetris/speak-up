# Speak Up

Speak Up is a personal English-speaking-practice tool I built to test coding with ChatGPT. It runs as a single HTML file and uses browser speech features, local feedback, LanguageTool, and an optional Gemini-powered C1 rewrite.

Open `speak-up.html` directly in Chrome to use it.
